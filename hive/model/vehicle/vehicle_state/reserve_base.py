# base = sim.bases[base_id]
# updated_base = base.checkout_stall()
# updated_vehicle = vehicle.transition(VehicleState.RESERVE_BASE)
# return sim.modify_vehicle(updated_vehicle).modify_station(updated_station).modify_base(updated_base)
